# 私有配置文件模板
# chezmoi 会将此文件应用到 ~/.config/private/private
# 使用时从环境变量读取敏感信息

# GitHub Token
export GITHUB_TOKEN="{{ .github.token }}"

# OpenAI API Key
export OPENAI_API_KEY="{{ .openai.apikey }}"

# 添加其他需要的环境变量
# export API_KEY="{{ .service.apikey }}"
